name: Shell Commands

on: [push]

jobs:
  run-shell-Command
    runs-on: ubuntu-latest
    steps:
      - name: echo string
        run: echo "Hello World"
      - name: multiline script
        run: |
          node -v
          npm -v